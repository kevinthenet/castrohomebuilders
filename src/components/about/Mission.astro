---
import { Image } from 'astro:assets';
import aboutImage from '../../assets/about.jpeg';

const stats = [
  {
    name: 'Established',
    value: '2002',
  },
  {
    name: 'Projects',
    value: '200+',
  },
  {
    name: 'Concurrent Project Teams',
    value: 5,
  },
  {
    name: 'Awards',
    value: '3',
  },
];
---

<section class="py-16 sm:py-20">
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="grid gap-16 lg:grid-cols-2 lg:gap-24">
      <Image
        class="mx-auto h-auto w-full max-w-xl rounded-3xl"
        src={aboutImage}
        alt="Our experts working together"
        loading="lazy"
      />
      <div class="flex flex-col gap-10">
        <!-- Mission description -->
        <div class="space-y-8 sm:space-y-12">
          <h2 class="text-3xl font-medium tracking-tight sm:text-4xl">Our Mission</h2>
          <div class="space-y-6">
            <p class="text-primary-950/70 dark:text-primary-200/70 text-base sm:text-lg">
              We here at Castro Home Builders aim to provide a courteous, professional experience in
              whatever work that needs to be done. Using only the highest quality materials and
              craftsmen, our goal is to help you make your dreams a reality on a timetable that
              suits you.
            </p>
          </div>
        </div>

        <!-- Stats -->
        <dl class="grid grid-cols-1 gap-6 sm:grid-cols-2">
          {
            stats.map((stat) => (
              <div class="border-primary-900/10 dark:border-primary-300/10 rounded-3xl border p-6">
                <dt class="text-base">{stat.name}</dt>
                <dd class="text-3xl font-medium tracking-tight">{stat.value}</dd>
              </div>
            ))
          }
        </dl>
      </div>
    </div>
  </div>
</section>
